import { InitCapsDirective } from './init-caps.directive';

describe('InitCapsDirective', () => {
  it('should create an instance', () => {
    const directive = new InitCapsDirective();
    expect(directive).toBeTruthy();
  });
});
