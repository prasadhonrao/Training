var category = "Music";
var id = 10;

// string must be enclose using backtick
var url = `http://gaana.com/${category}/${id}`;
console.log(url);

// multiline string can be declared
var multipleLines = `Roses are red
                    Voilets are blue`;
console.log(multipleLines);