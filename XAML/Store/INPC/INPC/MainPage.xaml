<Page
    x:Class="INPC.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:INPC"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="25">
        <StackPanel Orientation="Horizontal" >
            <TextBlock Text="{Binding Id}" FontSize="40" Padding="10" />
            <TextBlock Text="{Binding Name}" FontSize="40" Padding="10" />
        </StackPanel>
        <StackPanel>
            <Button HorizontalAlignment="Stretch" Content="Change Customer" Click="Button_Click"/>
        </StackPanel>
    </StackPanel>
</Page>
